name: ai_image_generator
description: "AI Image Generator"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0 # For API calls
  flutter_dotenv: ^5.1.0 # Environment variables
  flutter_spinkit: ^5.2.0 # Loading animations

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env # Required for web builds
